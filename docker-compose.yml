services:
  bagstack:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bagstack_app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - SMTP_USER=${SMTP_USER:-adityaraj212.work@gmail.com}
      - SMTP_PASS=${SMTP_PASS:-}
      - DATABASE_PATH=/app/data/finance.db
    volumes:
      - bagstack_data:/app/data

volumes:
  bagstack_data:
    driver: local
